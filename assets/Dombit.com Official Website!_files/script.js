function toggle() {
  // use jquery to select all the English elements
  $(".english, .french").toggleClass("hide");
  // use jquery to select all the french elements

  // toggle the hide class on both english and french
}

// class to selector to show French language web site starting
//use jquery
$("button").on("click", toggle);
// when web-page loads front page hide English show French only page

//
